# lunar-path
Lunar Path is a lightweight, read-only tool designed for inspecting and validating Base Sepolia. It provides a simple way to check network configuration, inspect addresses, and validate testnet contract deployments
